# =============================================================================
# DROID → DrRobot → Train pipeline (droid_drrobot_any4d_pipeline.py)
# =============================================================================
#
# DrRobot is used by path (--drrobot_root). This repo keeps a copy under
# wm/drrobot; the pipeline defaults to ./drrobot next to the script — symlink,
# copy, or pass --drrobot_root /path/to/drrobot.
#
# Headless rendering:
#   export MUJOCO_GL=egl
# =============================================================================

h5py>=3.8
scipy>=1.10

# --- DrRobot (wm/drrobot/README.md) ---
gsplat
filelock
mujoco
open3d
pytorch-kinematics
random-fourier-features-pytorch
pytz
gradio
ray
